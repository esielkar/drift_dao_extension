name: drift_dao_extension
description: Dao extension for drift package.
version: 1.0.4
repository: https://github.com/esielkar/drift_dao_extension

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  drift: ^2.26.0

dev_dependencies:
  build_runner: ^2.4.15
  drift_dev: ^2.26.0
  lints: ^5.0.0
  test: ^1.25.15
